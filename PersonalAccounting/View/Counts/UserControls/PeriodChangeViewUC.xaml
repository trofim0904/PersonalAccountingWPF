<UserControl x:Class="PersonalAccounting.View.Counts.UserControls.PeriodChangeViewUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonalAccounting.View.Counts.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="10"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <TextBlock Text="{Binding PeriodChange.CashCountActionCategory}"/>
            <TextBlock Text="{Binding PeriodChange.Sum}"/>
            <TextBlock Text="{Binding PeriodChange.ChangeDate}"/>
            <TextBlock Text="{Binding PeriodChange.Comment}"/>
            


        </StackPanel>
    </Grid>
</UserControl>
